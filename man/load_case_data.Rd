% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cases_interim.R
\name{load_case_data}
\alias{load_case_data}
\title{containing the \emph{target} \code{cases_per_1000} and \strong{only} the 12 static
covariates you specified, \strong{plus} the calendar year column.
Only loads data from timesteps 2190-4380 (which we map to years 2-7).}
\usage{
load_case_data(
  con,
  table_name,
  ts,
  param_limit = NULL,
  sim_limit = NULL,
  test_fraction = 0.2,
  seed = 42
)
}
\arguments{
\item{con}{A live \code{DBI} connection (e.g. \code{duckdb::duckdb()}).}

\item{table_name}{Character. Name of the table holding simulation output.}

\item{ts}{A list as returned by \code{get_timestep_window()} with
\code{data_start}, \code{start}, \code{end} (only \code{data_start} is used).}

\item{param_limit}{Optional integer.  Keep rows with
\code{parameter_index < param_limit}.}

\item{sim_limit}{Optional integer.  Randomly sample up to \code{sim_limit}
simulations \strong{per} \code{parameter_index}.}

\item{test_fraction}{Fraction of \emph{parameter indices} to allocate to the
test set (default \code{0.2}).}

\item{seed}{Integer random-seed for reproducible splits (default \code{42}).}
}
\description{
containing the \emph{target} \code{cases_per_1000} and \strong{only} the 12 static
covariates you specified, \strong{plus} the calendar year column.
Only loads data from timesteps 2190-4380 (which we map to years 2-7).
}
