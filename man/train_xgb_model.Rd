% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{train_xgb_model}
\alias{train_xgb_model}
\title{Train XGBoost with K-fold CV, QMAP+scale calibration, and optional artifacts}
\usage{
train_xgb_model(
  in_parquet,
  out_dir,
  thr_lo = 0.02,
  thr_hi = 0.95,
  k_strata = 16,
  K = 10,
  seed = 42,
  xgb_params = list(objective = "reg:squarederror", eval_metric = "rmse", tree_method =
    "hist", max_depth = 6, eta = 0.05, subsample = 0.8, colsample_bytree = 0.8,
    min_child_weight = 1, lambda = 1),
  nrounds_max = 5000,
  early_stopping_rounds = 100,
  save_rds = TRUE,
  export_onnx = FALSE,
  save_plots = TRUE,
  save_artifacts = TRUE
)
}
\arguments{
\item{in_parquet}{Path to input parquet}

\item{out_dir}{Base output directory (models/plots/metrics/predictions will be created)}

\item{thr_lo}{Lower prevalence filter (inclusive)}

\item{thr_hi}{Upper prevalence filter (inclusive)}

\item{k_strata}{Number of strata for k-means on log10(EIR)}

\item{K}{Number of CV folds}

\item{seed}{Random seed for reproducibility}

\item{xgb_params}{List of xgboost parameters}

\item{nrounds_max}{Max rounds per fold for early-stopped training}

\item{early_stopping_rounds}{Early stopping patience}

\item{save_rds}{Save an RDS bundle with model, calibrator, metadata}

\item{export_onnx}{Attempt ONNX export (will stop with message in R-only workflow)}

\item{save_plots}{Save diagnostic plots}

\item{save_artifacts}{Save CSV metrics and fold stats}
}
\value{
A list of class 'estiMINT_model' with booster, calibrator, features, metadata
}
\description{
Train XGBoost with K-fold CV, QMAP+scale calibration, and optional artifacts
}
